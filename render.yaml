services:
  - type: web
    name: se-fode-online
    runtime: node
    region: virginia
    plan: free
    branch: main
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /health
    autoDeployTrigger: off
